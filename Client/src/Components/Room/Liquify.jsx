function Liquify(arr,participants){

    let liquifiedArray = [];

    arr.forEach((payer) => {
        participants.forEach((participant) => {
            if(payer === participant){
                liquifiedArray.push({
                    
                })
            }
        })
    });
}